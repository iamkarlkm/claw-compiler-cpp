// Minimal IR pass benchmark harness placeholder.
// The full micro-benchmark suite lives in src/benchmark/; this file keeps the
// legacy test-ir-passes Makefile target buildable until the suite is expanded.

#include <cstdio>

int main() {
    std::printf("IR pass benchmark placeholder: OK\n");
    return 0;
}
