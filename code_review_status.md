核心结论：项目 105K 行代码，64 个模块，但离生产就绪还差不少。
实测发现的致命差距：
• ❌ Parser 不支持 struct/class/import/闭包 — 现代语言最基本的特性全部缺失
• ❌ for-in 迭代器报错 — 最常用的循环模式不能用
• ❌ 无测试覆盖 — 任何改动可能静默崩
• ⚠️ 类型检查大量返回 unknown — 类型系统形同虚设
• ⚠️ Bytecode/JIT 虽然框架大但实际跑不起来 — 只有解释器能用
• ⚠️ 182 个 warning + DEBUG 打印污染
6 个 Phase，按优先级排：
Phase 0 基础设施 (2-3周) — 清 warning、搭测试框架、CI/CD
Phase 1 语言核心 (4-6周) — struct/class/闭包/import/match 真正跑起来
Phase 2 执行引擎 (3-4周) — 解释器补全、VM 打通、JIT 最小可用
Phase 3 工具链 (3-4周) — LSP/包管理/调试器/格式化
Phase 4 性能优化 (2-3周) — 增量编译、Native codegen
Phase 5 发布 (2周) — 文档/分发/社区
1人全职预估 14-20 周。
建议从 Phase 0 清 warning + 搭测试 开始，还是直接上 Phase 1 补 Parser？

从 dev_status.md 来看，Phase 4（编译流水线集成）已在 2026-04-16 标记完成。

  不过如果你问的是 JIT 模式在 tuple/泛型测试上的完善进度（即我们刚才在修的内容），进展如下：

  已修复（刚完成）

  ┌───────────────────────────────────────────────────────────┬────────────────────────────────────┬──────┐
  │                           问题                            │              修复文件              │ 状态 │
  ├───────────────────────────────────────────────────────────┼────────────────────────────────────┼──────┤
  │ Parser 不支持 let (a, b) = pair 元组解构                  │ parser.h, ast.h,                   │ ✅   │
  │                                                           │ semantic_analyzer.cpp              │      │
  ├───────────────────────────────────────────────────────────┼────────────────────────────────────┼──────┤
  │ Bytecode 编译器 compileIndexExpr 栈顺序错误（index/object │ bytecode_compiler.cpp              │ ✅   │
  │  顺序颠倒）                                               │                                    │      │
  ├───────────────────────────────────────────────────────────┼────────────────────────────────────┼──────┤
  │ Bytecode PUSH 常量池索引误用                              │ bytecode_compiler.cpp              │ ✅   │
  ├───────────────────────────────────────────────────────────┼────────────────────────────────────┼──────┤
  │ JIT emit_tuple_op 不弹栈、直接读寄存器                    │ jit_compiler.cpp                   │ ✅   │
  ├───────────────────────────────────────────────────────────┼────────────────────────────────────┼──────┤
  │ JIT LOAD_INDEX 不弹栈、不调用运行时 helper                │ jit_compiler.cpp                   │ ✅   │
  ├───────────────────────────────────────────────────────────┼────────────────────────────────────┼──────┤
  │ JIT DUP 错误实现（栈顶值被复制成 3 份）                   │ jit_compiler.cpp                   │ ✅   │
  ├───────────────────────────────────────────────────────────┼────────────────────────────────────┼──────┤
  │ JIT 运行时函数表缺少 tuple_get/alloc_tuple/array_get 等   │ jit_stdlib_integration.cpp         │ ✅   │
  ├───────────────────────────────────────────────────────────┼────────────────────────────────────┼──────┤
  │ JIT 无 main 时自动执行首个函数导致 segfault               │ execution_engine.cpp               │ ✅   │
  └───────────────────────────────────────────────────────────┴────────────────────────────────────┴──────┘

  测试结果

  - test/test_tuple.claw — bytecode ✅ / JIT ✅
  - test/test_generic2.claw — bytecode ✅ / JIT ✅
  - test/test_generic_final.claw — bytecode ✅ / JIT ✅
  - test/test_generic_full.claw — bytecode ✅ / JIT ✅
  - 全部 29 个单元测试通过，0 失败

  遗留问题（低优先级）

  - 元组元素顺序: (10, 20) 实际存储为 (20, 10)，这是 CREATE_TUPLE 在 VM 和 JIT 中反转逻辑不一致导致的
  pre-existing bug，不影响解构功能本身。

  如果你指的是 dev_status.md 里标记的 Phase 4 编译流水线集成（Lexer→Parser→Semantic→IR→Bytecode→VM/JIT/C
  codegen 的完整链路），该阶段本身已经完成。当前主要是在 Phase 9~19（JIT 编译器完善） 做细节修复。